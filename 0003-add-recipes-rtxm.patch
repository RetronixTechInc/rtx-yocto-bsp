---
 .../recipes-rtxm/rtxm/rtxm-01/clean_touch     | 38 +++++++++++++++++++
 .../recipes-rtxm/rtxm/rtxm-01/reset_touch     | 38 +++++++++++++++++++
 meta-bsp/recipes-rtxm/rtxm/rtxm_01.bb         | 34 +++++++++++++++++
 3 files changed, 110 insertions(+)
 create mode 100644 meta-bsp/recipes-rtxm/rtxm/rtxm-01/clean_touch
 create mode 100644 meta-bsp/recipes-rtxm/rtxm/rtxm-01/reset_touch
 create mode 100644 meta-bsp/recipes-rtxm/rtxm/rtxm_01.bb

diff --git a/meta-bsp/recipes-rtxm/rtxm/rtxm-01/clean_touch b/meta-bsp/recipes-rtxm/rtxm/rtxm-01/clean_touch
new file mode 100644
index 0000000000..08a14261ea
--- /dev/null
+++ b/meta-bsp/recipes-rtxm/rtxm/rtxm-01/clean_touch
@@ -0,0 +1,38 @@
+#!/bin/sh
+
+CALVAL="1.051360 -0.003640 -0.030474 0.016130 1.112044 -0.074869"
+
+FILE=/etc/udev/rules.d/touchscreen.rules
+
+if [ -f $FILE ]; then
+	rm $FILE
+	sync
+fi
+
+echo -e "# There are a number of modifiers that are allowed to be used in some"								>> $FILE
+echo -e "# of the different fields. They provide the following subsitutions:"								>> $FILE
+echo -e "#"																>> $FILE
+echo -e "# %n the \"kernel number\" of the device."											>> $FILE
+echo -e "#    For example, 'sda3' has a \"kernel number\" of '3'"									>> $FILE
+echo -e "# %e the smallest number for that name which does not matches an existing node"						>> $FILE
+echo -e "# %k the kernel name for the device"												>> $FILE
+echo -e "# %M the kernel major number for the device"											>> $FILE
+echo -e "# %m the kernel minor number for the device"											>> $FILE
+echo -e "# %b the bus id for the device"												>> $FILE
+echo -e "# %c the string returned by the PROGRAM"											>> $FILE
+echo -e "# %s{filename} the content of a sysfs attribute"										>> $FILE
+echo -e "# %% the '%' char itself"													>> $FILE
+echo -e "#"																>> $FILE
+echo -e ""																>> $FILE
+echo -e "# Create a symlink to any touchscreen input device"										>> $FILE
+echo -e "SUBSYSTEM==\"input\", KERNEL==\"event[0-9]*\", ATTRS{modalias}==\"input:*-e0*,3,*a0,1,*18,*\", SYMLINK+=\"input/touchscreen0\"">> $FILE
+echo -e "SUBSYSTEM==\"input\", KERNEL==\"event[0-9]*\", ATTRS{modalias}==\"ads7846\", SYMLINK+=\"input/touchscreen0\""			>> $FILE
+echo -e "# i.MX specific touchscreen rules"												>> $FILE
+echo -e "SUBSYSTEM==\"input\", KERNEL==\"event[0-9]*\", ENV{ID_INPUT_TOUCHSCREEN}==\"1\", SYMLINK+=\"input/touchscreen0\""		>> $FILE
+#echo -e "SUBSYSTEM==\"input\", KERNEL==\"event[0-9]*\", ENV{ID_INPUT_TOUCHSCREEN}==\"1\", ENV{LIBINPUT_CALIBRATION_MATRIX}=\"$CALVAL\""	>> $FILE
+
+sync
+sync
+
+reboot
+
diff --git a/meta-bsp/recipes-rtxm/rtxm/rtxm-01/reset_touch b/meta-bsp/recipes-rtxm/rtxm/rtxm-01/reset_touch
new file mode 100644
index 0000000000..45c6f08e11
--- /dev/null
+++ b/meta-bsp/recipes-rtxm/rtxm/rtxm-01/reset_touch
@@ -0,0 +1,38 @@
+#!/bin/sh
+
+CALVAL="1.051360 -0.003640 -0.030474 0.016130 1.112044 -0.074869"
+
+FILE=/etc/udev/rules.d/touchscreen.rules
+
+if [ -f $FILE ]; then
+	rm $FILE
+	sync
+fi
+
+echo -e "# There are a number of modifiers that are allowed to be used in some"								>> $FILE
+echo -e "# of the different fields. They provide the following subsitutions:"								>> $FILE
+echo -e "#"																>> $FILE
+echo -e "# %n the \"kernel number\" of the device."											>> $FILE
+echo -e "#    For example, 'sda3' has a \"kernel number\" of '3'"									>> $FILE
+echo -e "# %e the smallest number for that name which does not matches an existing node"						>> $FILE
+echo -e "# %k the kernel name for the device"												>> $FILE
+echo -e "# %M the kernel major number for the device"											>> $FILE
+echo -e "# %m the kernel minor number for the device"											>> $FILE
+echo -e "# %b the bus id for the device"												>> $FILE
+echo -e "# %c the string returned by the PROGRAM"											>> $FILE
+echo -e "# %s{filename} the content of a sysfs attribute"										>> $FILE
+echo -e "# %% the '%' char itself"													>> $FILE
+echo -e "#"																>> $FILE
+echo -e ""																>> $FILE
+echo -e "# Create a symlink to any touchscreen input device"										>> $FILE
+echo -e "SUBSYSTEM==\"input\", KERNEL==\"event[0-9]*\", ATTRS{modalias}==\"input:*-e0*,3,*a0,1,*18,*\", SYMLINK+=\"input/touchscreen0\"">> $FILE
+echo -e "SUBSYSTEM==\"input\", KERNEL==\"event[0-9]*\", ATTRS{modalias}==\"ads7846\", SYMLINK+=\"input/touchscreen0\""			>> $FILE
+echo -e "# i.MX specific touchscreen rules"												>> $FILE
+echo -e "SUBSYSTEM==\"input\", KERNEL==\"event[0-9]*\", ENV{ID_INPUT_TOUCHSCREEN}==\"1\", SYMLINK+=\"input/touchscreen0\""		>> $FILE
+echo -e "SUBSYSTEM==\"input\", KERNEL==\"event[0-9]*\", ENV{ID_INPUT_TOUCHSCREEN}==\"1\", ENV{LIBINPUT_CALIBRATION_MATRIX}=\"$CALVAL\""	>> $FILE
+
+sync
+sync
+
+reboot
+
diff --git a/meta-bsp/recipes-rtxm/rtxm/rtxm_01.bb b/meta-bsp/recipes-rtxm/rtxm/rtxm_01.bb
new file mode 100644
index 0000000000..53dc1cb2fb
--- /dev/null
+++ b/meta-bsp/recipes-rtxm/rtxm/rtxm_01.bb
@@ -0,0 +1,34 @@
+# Copyright (C) 2017-2018 RTX-3110
+
+SUMMARY = "Rtx modify"
+DESCRIPTION = "Add for customer requirements"
+HOMEPAGE = ""
+LICENSE = "CLOSED"
+
+inherit allarch
+
+SRC_URI = "file://touch.png \
+           file://touch_24x24.png \
+           file://touch_48x48.png \
+           file://reset_touch \
+           file://clean_touch \
+           "
+
+FILES:${PN} += " \
+/usr/share/icons/apps/touch.png \
+/usr/share/icons/apps/touch_24x24.png \
+/usr/share/icons/apps/touch_48x48.png \
+/usr/bin/reset_touch \
+/usr/bin/clean_touch \
+"
+
+do_install() {
+	install -d ${D}/usr/share/icons/apps
+	install -m 0755 ${WORKDIR}/touch.png ${D}/usr/share/icons/apps
+	install -m 0755 ${WORKDIR}/touch_24x24.png ${D}/usr/share/icons/apps
+	install -m 0755 ${WORKDIR}/touch_48x48.png ${D}/usr/share/icons/apps
+	install -d ${D}/usr/bin
+	install -m 0755 ${WORKDIR}/reset_touch ${D}/usr/bin
+	install -m 0755 ${WORKDIR}/clean_touch ${D}/usr/bin
+}
+
-- 
2.17.1

